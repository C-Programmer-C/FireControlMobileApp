<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:backgroundTint="@color/white"
    android:background="@color/white"
    tools:context=".Login_activity">

    <Button
        android:id="@+id/button"
        android:layout_width="222dp"
        android:layout_height="49dp"
        android:layout_marginTop="8dp"
        android:text="@string/login_button"
        android:backgroundTint="@color/main_orange"
        app:layout_constraintEnd_toEndOf="@+id/login_etPass"
        app:layout_constraintStart_toStartOf="@+id/login_etPass"
        app:layout_constraintTop_toBottomOf="@+id/login_etPass" />

    <EditText
        android:id="@+id/login_etEmail"
        android:layout_width="310dp"
        android:layout_height="65dp"
        android:background="@drawable/back_login_edit_text"
        android:ems="10"
        android:hint="@string/login_et_email"
        android:inputType="textEmailAddress"
        android:padding="10dp"
        android:textColor="@color/black"
        android:textColorHint="@color/gray"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.507"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.413" />

    <EditText
        android:id="@+id/login_etPass"
        android:layout_width="310dp"
        android:layout_height="65dp"
        android:layout_marginTop="10dp"
        android:layout_marginBottom="300dp"
        android:background="@drawable/back_login_edit_text"
        android:ems="10"
        android:hint="@string/login_et_pass"
        android:inputType="textPassword"
        android:padding="10dp"
        android:textColorHint="@color/gray"
        android:textColor="@color/black"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@+id/login_etEmail"
        app:layout_constraintHorizontal_bias="0.571"
        app:layout_constraintStart_toStartOf="@+id/login_etEmail"
        app:layout_constraintTop_toBottomOf="@+id/login_etEmail"
        app:layout_constraintVertical_bias="0.142" />

    <TextView
        android:id="@+id/login_tvRegestration"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="5dp"
        android:text="@string/login_tvRegestration"
        app:layout_constraintEnd_toEndOf="@+id/button"
        app:layout_constraintStart_toStartOf="@+id/button"
        app:layout_constraintTop_toBottomOf="@+id/button" />

</androidx.constraintlayout.widget.ConstraintLayout>